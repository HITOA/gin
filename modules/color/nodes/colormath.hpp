#pragma once

#include <gin/gin.hpp>

namespace Gin::Module::Color {

    /*
	class Blend : public Graph::Node {
	public:
		Blend();

		virtual void Execute(Graph::GraphContext ctx) final;
		virtual void Execute(Graph::GraphContext ctx, Thread::ThreadPool& pool) final;

		virtual std::string GetName() final;

	private:
		Spatial::Spatial<Eigen::Vector4<float>> a{};
		Spatial::Spatial<Eigen::Vector4<float>> b{};
		Spatial::Spatial<float> t{};

		Spatial::Spatial<Eigen::Vector4<float>> color{};
	};*/

}