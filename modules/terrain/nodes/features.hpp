#pragma once

#include <gin/graph/node.hpp>

/*
namespace Gin::Module::Terrain {

	class Overhang : public Gin::Graph::Node {
	public:
		Overhang();

		virtual void Execute(Gin::Graph::GraphContext ctx) final;
		virtual void Execute(Gin::Graph::GraphContext ctx, Thread::ThreadPool& pool) final;

		virtual std::string GetName() final;

	private:
		Spatial::Spatial<float> base{ 0.0f };
		Spatial::Spatial<float> amplitude{ 16.0f };
		Spatial::Spatial<float> mask{};
		Spatial::Spatial<float> midLevel{ 0.0f };
		Spatial::Spatial<float> slopWidth{ 0.5f };
		Spatial::Spatial<float> slopProfile{ 1.0f };
		Spatial::Spatial<Eigen::Vector3<double>> position{};

		Spatial::Spatial<float> distance{};
	};

}
*/